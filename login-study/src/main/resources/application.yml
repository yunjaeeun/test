spring:
  # DB ?? ?? ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: mariadb
  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true